#!/usr/bin/env node

var WebSocketHook = require('../lib/hookws').HookSocket;

var mtgoxTest = new WebSocketHook({
  name: 'mtgox',
  url:'ws://websocket.mtgox.com/mtgox',
  debug:true
});


mtgoxTest.on('websocket::message',function(data){
  if (data.op == 'subscribe'){
    console.log('Now suscribe to ');
  }
  console.log(data);
});

mtgoxTest.start();
